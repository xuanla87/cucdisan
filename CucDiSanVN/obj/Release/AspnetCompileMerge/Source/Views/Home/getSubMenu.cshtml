@using CucDiSanService.Models
@model List<Menu>
@if (Model != null && Model.Count > 0)
{
    <ul class="sub-menu nav">
        @foreach (var item in Model)
        {
            <li>
                <a href="@item.menuUrl">@item.menuName</a>
                @Html.Action("getSubMenu", "Home", new { Id = item.menuId })
            </li>
        }
    </ul>
}

